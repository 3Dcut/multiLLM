<!DOCTYPE html>
<html>
<head>
    <title>LLM MultiChat - Nutzungsbedingungen</title>
    <HTA:APPLICATION
        ID="DisclaimerWindow"
        APPLICATIONNAME="LLM MultiChat Disclaimer"
        BORDER="dialog"
        BORDERSTYLE="normal"
        CAPTION="yes"
        SHOWINTASKBAR="yes"
        SINGLEINSTANCE="yes"
        SYSMENU="no"
        WINDOWSTATE="normal"
        SCROLL="no"
    />
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, sans-serif;
            background: #f5f5f5;
            color: #333;
            margin: 0;
            padding: 20px;
            font-size: 13px;
        }
        h1 {
            margin: 0 0 15px 0;
            font-size: 18px;
            color: #1a1a2e;
            border-bottom: 2px solid #6366f1;
            padding-bottom: 10px;
        }
        .warning-box {
            background: #fff3cd;
            border: 1px solid #ffc107;
            border-left: 4px solid #ffc107;
            padding: 15px;
            margin-bottom: 15px;
            border-radius: 4px;
        }
        .warning-box h2 {
            margin: 0 0 10px 0;
            font-size: 14px;
            color: #856404;
        }
        .content {
            background: white;
            border: 1px solid #ddd;
            padding: 15px;
            margin-bottom: 15px;
            border-radius: 4px;
            max-height: 200px;
            overflow-y: auto;
        }
        .checkbox-container {
            background: #e8f4fd;
            border: 1px solid #bee5eb;
            padding: 12px;
            margin-bottom: 15px;
            border-radius: 4px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .checkbox-container input {
            width: 18px;
            height: 18px;
            cursor: pointer;
        }
        .checkbox-container label {
            cursor: pointer;
            font-weight: 500;
        }
        .hint {
            background: #f8d7da;
            border: 1px solid #f5c6cb;
            color: #721c24;
            padding: 10px;
            margin-bottom: 15px;
            border-radius: 4px;
            font-size: 12px;
        }
        .buttons {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
        }
        .btn {
            padding: 10px 25px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 13px;
            font-weight: 500;
        }
        .btn-security {
            background: #6c757d;
            color: white;
        }
        .btn-security:hover {
            background: #5a6268;
        }
        .btn-ok {
            background: #dc3545;
            color: white;
        }
        .btn-ok:hover {
            background: #c82333;
        }
        .btn-cancel {
            background: #28a745;
            color: white;
        }
        .btn-cancel:hover {
            background: #218838;
        }
        .btn:disabled {
            background: #ccc;
            cursor: not-allowed;
        }
        .right-buttons {
            display: flex;
            gap: 10px;
        }
    </style>
    <script>
        var fso, shell, appPath, resultFile;
        
        window.resizeTo(550, 520);
        window.moveTo((screen.width - 550) / 2, (screen.height - 520) / 2);
        
        try {
            fso = new ActiveXObject("Scripting.FileSystemObject");
            shell = new ActiveXObject("WScript.Shell");
            
            // Pfad ermitteln
            var fullPath = window.location.pathname;
            if (fullPath.charAt(0) === '/') fullPath = fullPath.substring(1);
            fullPath = fullPath.replace(/\//g, '\\');
            fullPath = unescape(fullPath);
            appPath = fso.GetParentFolderName(fullPath);
            
            resultFile = shell.ExpandEnvironmentStrings("%TEMP%") + "\\llm-multichat-disclaimer.txt";
        } catch(e) {
            alert("Fehler: " + e.message);
        }
        
        function updateButtons() {
            var checked = document.getElementById('acceptCheckbox').checked;
            document.getElementById('btnCancel').disabled = !checked;
        }
        
        function openSecurityReport() {
            try {
                var pdfPath = appPath + "\\security-report.pdf";
                if (fso.FileExists(pdfPath)) {
                    shell.Run('"' + pdfPath + '"', 1, false);
                } else {
                    alert("security-report.pdf nicht gefunden.\n\nErwartet in:\n" + pdfPath);
                }
            } catch(e) {
                alert("Fehler beim Oeffnen: " + e.message);
            }
        }
        
        function doCancel() {
            // "Abbrechen" = Akzeptieren (Lesetest bestanden!)
            try {
                var file = fso.CreateTextFile(resultFile, true);
                file.WriteLine("ACCEPTED");
                file.Close();
            } catch(e) {}
            window.close();
        }
        
        function doOK() {
            // "OK" = Ablehnen (Lesetest nicht bestanden)
            try {
                var file = fso.CreateTextFile(resultFile, true);
                file.WriteLine("DECLINED");
                file.Close();
            } catch(e) {}
            window.close();
        }
        
        window.onload = function() {
            updateButtons();
        };
    </script>
</head>
<body>
    <h1>⚠️ LLM MultiChat - Nutzungsbedingungen</h1>
    
    <div class="warning-box">
        <h2>WICHTIGER HINWEIS</h2>
        Dieses Programm wurde zu <strong>100% von kuenstlicher Intelligenz generiert</strong> 
        und ist <strong>ungepruefte Software</strong>.
    </div>
    
    <div class="content">
        <p><strong>Mit der Nutzung dieser Software erklaeren Sie sich einverstanden:</strong></p>
        <ul>
            <li>Der gesamte Quellcode wurde von KI-Systemen (Claude) erstellt</li>
            <li>Es wurde <strong>keine manuelle Codepruefung</strong> durchgefuehrt</li>
            <li>Es gibt <strong>keine Garantie</strong> fuer Funktionalitaet oder Sicherheit</li>
            <li>Die Nutzung erfolgt <strong>auf eigene Gefahr und Verantwortung</strong></li>
            <li>Der Autor uebernimmt <strong>keine Haftung</strong> fuer Schaeden jeglicher Art</li>
            <li>Diese Software ist <strong>nicht fuer den produktiven Einsatz</strong> gedacht</li>
            <li>Sensible Daten sollten <strong>nicht</strong> ueber diese Anwendung verarbeitet werden</li>
        </ul>
        <p><strong>Empfehlung:</strong> Lesen Sie den Security-Report, bevor Sie fortfahren.</p>
    </div>
    
    <div class="checkbox-container">
        <input type="checkbox" id="acceptCheckbox" onclick="updateButtons()">
        <label for="acceptCheckbox">Ich habe die Hinweise gelesen und akzeptiere die Nutzung auf eigene Verantwortung</label>
    </div>
    
    <div class="hint">
        <strong>📖 Lesetest:</strong> Wenn Sie die Bedingungen akzeptieren moechten, 
        druecken Sie <strong>"Abbrechen"</strong> um fortzufahren.
    </div>
    
    <div class="buttons">
        <button class="btn btn-security" onclick="openSecurityReport()">📄 Security-Report</button>
        <div class="right-buttons">
            <button class="btn btn-ok" onclick="doOK()">OK</button>
            <button class="btn btn-cancel" id="btnCancel" onclick="doCancel()" disabled>Abbrechen</button>
        </div>
    </div>
</body>
</html>
